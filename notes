 set resolutions of sddm and grub in /usr/share/sddm/scripts/Xsetup and /etc/default/grub 
 gtk themes: gruvbox(), everblush(
 	git clone https://github.com/Everblush/gtk
	cd gtk
	npm install -g scss
	sudo make install
 ), catppuccin()
 gsettings set org.gnome.desktop.interface color-scheme 'prefer-dark'

collect nvm and npm -g installs

pip install git+https://github.com/Fabric-Development/fabric.git after creating venv
pip install pygobject-stubs #### for auto completions

sudo systemctl enable+start libvirtd.service for kvm and smooth emulator
sudo usermod -aG libvirt $(whoami) for kvm use without root
then sudo systemctl restart libvirtd.service

###########
for wine to be working correctly
sudo pacman -Sy --needed \
  lib32-glibc \
  lib32-gcc-libs \
  lib32-libx11 \
  lib32-libxext \
  lib32-mesa \
  lib32-freetype2 \
  lib32-fontconfig \
  lib32-libpulse
also
sudo pacman -S --needed \
  lib32-gnutls \
  lib32-mesa \
  lib32-mesa-utils \
  lib32-libglvnd \
  lib32-libxcomposite \
  lib32-libxrandr \
  lib32-libxi \
  lib32-libxinerama \
  lib32-freetype2
  libxml2-legacy
  lib32-libxml2-legacy
#################
